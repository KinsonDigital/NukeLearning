name: ✅Build Status Check

on:
  workflow_dispatch:
  pull_request:
    

jobs:
  build_project:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run './build.cmd CompileAllProjects'
        run: ./build.cmd CompileAllProjects
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
